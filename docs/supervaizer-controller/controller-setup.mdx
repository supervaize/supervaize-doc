---
id: controller-setup
title: Controller Setup Guide
displayed_sidebar: supervaizerControllerSidebar
slug: controller-setup
---

import Link from "@docusaurus/Link";
import "bootstrap-icons/font/bootstrap-icons.css";

This guide walks you through customizing your Supervaizer controller step by step.

## Overview

:::info

This step-by-step guide uses the [<i className="bi bi-github"></i> Hello World Agent](https://github.com/runwaize/supervaize_hello_world) as a reference example.

See the [Quickstart guide](quickstart) for installation instructions.
:::

After running `supervaizer scaffold`, you'll have a template file to customize. This guide shows you how to:

[<i className="bi bi-1-circle-fill"></i> Define agent parameters: ParameterSetup()](#1)

[<i className="bi bi-2-circle-fill"></i> Define agent methods: AgentMethod()](#2)

[<i className="bi bi-3-circle-fill"></i> Define the **fields** of the AgentMethods: AgentMethodField()](#3)

[<i className="bi bi-4-circle-fill"></i> Declare the agent: Agent()](#4)

[<i className="bi bi-5-circle-fill"></i> Define the account: Account()](#5)

[<i className="bi bi-6-circle-fill"></i> Setup the server: Server()](#6)

:::note
Open `supervaizer_control_example.py` and adapt the content to your agent.
:::

## 1. Define agent parameters {#1}

Use **Parameter** / **ParametersSetup** to declare what the agent needs. These show up in Supervaize UI and can be marked as env or secret.

```js reference title="Map Parameters"
https://github.com/runwaize/supervaize_hello_world/blob/main/supervaizer_control.py#L10-L30
```

**📖 See the model references**

[Parameter Documentation](/docs/supervaizer-controller/model_reference/model_core#parameterparameterabstract)

[ParameterSetup Documentation](/docs/supervaizer-controller/model_reference/model_core#parameterparameterssetup)

<details>
<summary>**Prompt: Define agent parameters**</summary>

```text
In my supervaizer_control.py, define the agent parameters using ParametersSetup and Parameter from the supervaizer package.

My agent needs the following configuration:
- [Describe each parameter: name, description, whether it's an environment variable, whether it's a secret]

Use ParametersSetup.from_list([...]) with Parameter objects.
Set is_environment=True for env variables and is_secret=True for secrets.
```

</details>

## 2. Define agent methods {#2}

Point Supervaizer to your agent's functions.

:::tip
It usually saves time to create a wrapper module where your agent's methods are called with the expected parameters.
:::

```python title="Methods: wire your agent's functions"
from supervaizer import AgentMethod

job_start = AgentMethod(
    name="start",  # This is required
    method="agent_simple.job_start",  # Path to the main function in dotted notation
    params={},  # Default parameters to pass to the function
    is_async=False,  # Only use sync methods for the moment
    fields=[]  # Defined below
)
```

[See below for definition of fields ](#3)

```python title="Additional required methods"
job_stop = AgentMethod(
    name="stop",  # Must be present
    method="agent_simple.job_stop",
    is_async=False,
)

job_status = AgentMethod(
    name="status",  # Must be present
    method="agent_simple.job_status",
    is_async=False,
)
```

**📖 See the model reference**

[ParameterModel Documentation](/docs/supervaizer-controller/model_reference/model_core#agentagentmethodabstract)

<details>
<summary>**Prompt: Define agent methods**</summary>

```text
In my supervaizer_control.py, define the AgentMethod entries that wire Supervaizer to my agent's functions.

My agent module is called [my_agent_module] and exposes these functions:
- [function_name] for starting jobs
- [function_name] for stopping jobs
- [function_name] for checking status

Create three AgentMethod objects:
1. job_start with name="start", method="[my_agent_module].[start_function]", is_async=False, params and fields (fields will be defined next)
2. job_stop with name="stop", method="[my_agent_module].[stop_function]", is_async=False
3. job_status with name="status", method="[my_agent_module].[status_function]", is_async=False

The "start" and "stop" and "status" method names are required by Supervaizer.
```

</details>

## 3. Define the fields of the AgentMethods {#3}

The `fields` parameter defines the user input form that will be displayed in the Supervaize UI. Each field specifies what data users need to provide when calling your agent method.

### Field setup for the "Start" method

```js reference title="Fields of Start AgentMethod"
https://github.com/runwaize/supervaize_hello_world/blob/main/supervaizer_control.py#L32-L60
```

**📖 See the model reference**

[AgentMethodField Documentation](/docs/supervaizer-controller/model_reference/model_core#agentagentmethodfield)

<details>
<summary>**Prompt: Define AgentMethod fields**</summary>

```text
In my supervaizer_control.py, define the AgentMethodField entries for the "start" method's fields parameter.

These fields define the user input form displayed in the Supervaize UI when a job is launched.

My start method needs the following user inputs:
- [Field name, type (str/int/float/bool), field_type (CharField/IntegerField/TextField/BooleanField), required or optional]

Create AgentMethodField objects and assign them to the fields=[] list of the job_start AgentMethod.
Each field needs: name, type, field_type, and required.
```

</details>

## 4. Declare the agent {#4}

The `Agent` class defines your agent's metadata and capabilities. This is where you specify what your agent does, who maintains it, and how it can be discovered.

```js reference title="Agent definition"
https://github.com/runwaize/supervaize_hello_world/blob/main/supervaizer_control.py#L62-L85
```

**📖 See the model reference**

[Agent Documentation](/docs/supervaizer-controller/model_reference/model_core#agentagentabstract)

<details>
<summary>**Prompt: Declare the agent**</summary>

```text
In my supervaizer_control.py, declare the Agent instance that registers my agent with Supervaizer.

Use the following metadata:
- name: [My Agent Name]
- author: [Author Name <email>]
- version: [version string, e.g. "1.0"]
- description: [What the agent does]
- tags: [list of tags for discovery, e.g. "automation", "data processing"]

Generate a deterministic ID using shortuuid.uuid(name).
Wire the previously defined methods using AgentMethods(job_start=..., job_stop=..., job_status=...).
Attach the parameters_setup defined in step 1.
```

</details>

## 5.Setup the Supervaize Account and configure your Environment variables {#5}

:::info
This step is not needed to run the supervaizer as an A2A server only. See [Protocol support](/docs/supervaizer-controller/ref/PROTOCOLS)
:::

[Create your developer account](/docs/supervaize-fleet/developer-account#getting_started) on the Supervaize platform.

Create your API Key and collect your environment variables.

See the [Quickstart guide](quickstart#3) to setup the environment variables

```js reference title="Account definition"
https://github.com/runwaize/supervaize_hello_world/blob/main/supervaizer_control.py#L87-L95
```

**📖 See the model reference**

[Account Documentation](/docs/supervaizer-controller/model_reference/model_core#accountaccountabstract)

<details>
<summary>**Prompt: Setup the account**</summary>

```text
In my supervaizer_control.py, define the Supervaize Account using environment variables.

Create an Account instance with:
- workspace_id from SUPERVAIZE_WORKSPACE_ID env var (default to a dummy value)
- api_key from SUPERVAIZE_API_KEY env var (default to a dummy value)
- api_url from SUPERVAIZE_API_URL env var (default to "https://app.supervaize.com")

Use os.getenv() with fallback defaults to prevent errors when env vars are missing.
```

</details>

## 6. Setup the Server {#6}

### Config

The `Server` class wraps all the defined components together.

At the minimum, the server requires at least one agent to be defined and the supervisor account (we are using Supervaize as the supervisor).

```js reference title="Server definition"
https://github.com/runwaize/supervaize_hello_world/blob/main/supervaizer_control.py#L97-L105
```

:::warning Important
Note that if `supervisor_account` is provided, the A2A protocol is automatically activated
:::

**📖 See the model reference**

[Server Documentation](/docs/supervaizer-controller/model_reference/model_core#serverserverabstract)

### Server launch

```js reference title="Server launch"
https://github.com/runwaize/supervaize_hello_world/blob/main/supervaizer_control.py#L107-L115
```

See [Server Start 🚀](/docs/supervaizer-controller/quickstart#start) for CLI instructions

<details>
<summary>**Prompt: Setup the server**</summary>

```text
In my supervaizer_control.py, define the Server instance and the launch entry point.

Create a Server with:
- agents: list of my Agent instances defined in step 4
- a2a_endpoints: True to enable A2A protocol discovery
- supervisor_account: the Account instance defined in step 5

Expose the FastAPI app for deployment:
  app = sv_server.app

Add the main entry point:
  if __name__ == "__main__":
      sv_server.launch(log_level="DEBUG")
```

</details>

## What's next?

- Learn about [Application Flow Control](application-flow-control) to manage cases, track progress, and handle human input
- Return to the [Quickstart](/docs/supervaizer-controller/quickstart) to continue with configuration and deployment
- Check the [Model Reference](/docs/supervaizer-controller/model_reference/model_core) for detailed field specifications
- Explore [API Reference](/docs/supervaizer-controller/ref/API_REFERENCE) for advanced usage
